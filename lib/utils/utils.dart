import 'package:flutter/material.dart';

const appBarColor = Colors.green;
const appBarStyleText = TextStyle(fontSize: 25.0, fontWeight: FontWeight.w600, color: Colors.white);
const ButtonColor = Colors.green;
const buttonText = TextStyle(fontSize: 16.0, color: Colors.white, fontWeight: FontWeight.w600);
final containerButton = BoxDecoration(
  borderRadius: BorderRadius.circular(15.0),
  color: ButtonColor,
);

